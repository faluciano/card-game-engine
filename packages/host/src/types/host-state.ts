// ─── Host Bridge State (re-exported from @card-engine/shared) ─────
// Types live in shared so both host and client can use them.

export type { HostScreen, HostGameState, HostAction } from "@card-engine/shared";
