// ─── Ruleset Picker Screen ─────────────────────────────────────────
// Displays available rulesets and allows import of new ones.

import React from "react";

export function RulesetPicker(): React.JSX.Element {
  // TODO: Load rulesets from RulesetStore
  // TODO: Display list with name, author, player count
  // TODO: Import button → FileImporter / URLImporter
  // TODO: Select → navigate to Lobby
  return (
    <div data-testid="ruleset-picker">
      <h1>Choose a Game</h1>
    </div>
  );
}
