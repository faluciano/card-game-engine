// ─── Lobby Screen ──────────────────────────────────────────────────
// Waiting room where players connect before a game starts.

import React from "react";

export function Lobby(): React.JSX.Element {
  // TODO: Display QR code / connection info for clients
  // TODO: Show connected players list
  // TODO: Start game button (enabled when min players reached)
  // TODO: Role assignment
  return (
    <div data-testid="lobby">
      <h1>Lobby</h1>
    </div>
  );
}
